version: "3.4"

networks:
  proxynetwork:
    external: true
  net:
    driver: overlay
    attachable: true

volumes:
  mariadb_data:

services:
  redis:
    image: redis:6.0.9
    networks:
      - net
    deploy:
      labels:
        - "traefik.enable=false"
  mailhog:
    image: mailhog/mailhog
    networks:
      - net
      - proxynetwork
    depends_on:
      - proxynetwork
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.mailhog-wordpress.rule=Host(`mailhog.wordpress.lxc`)"
        - "traefik.http.routers.mailhog-wordpress.entrypoints=web"
        - "traefik.http.services.mailhog-wordpress.loadbalancer.server.port=8025"
  mercure:
    image: dunglas/mercure
    environment:
      JWT_KEY: "MyJwtSecret"
      ALLOW_ANONYMOUS: 1
      PUBLISH_ALLOWED_ORIGINS: "*"
      CORS_ALLOWED_ORIGINS: "*"
    networks:
      - net
      - proxynetwork
    depends_on:
      - proxynetwork
      - redis
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.mercure-wordpress.rule=Host(`mercure.wordpress.lxc`)"
        - "traefik.http.routers.mercure-wordpress.entrypoints=web"
        - "traefik.http.services.mercure-wordpress.loadbalancer.server.port=80"
  mariadb:
    image: mariadb:10.5.6
    environment:
      MYSQL_ROOT_PASSWORD: example
      MYSQL_DATABASE: core
      MYSQL_USER: siteinternet
      MYSQL_PASSWORD: password
      TZ: Europe/Paris
    networks:
      - net
    deploy:
      labels:
        - "traefik.enable=false"
  apache:
    image: httpd
    depends_on:
      - phpfpm
    volumes:
      - ${PWD}/apps:/var/www/
      - ${PWD}/conf/httpd.conf:/usr/local/apache2/conf/httpd.conf
    networks:
      - net
      - proxynetwork
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.www-wordpress.rule=Host(`wordpress.lxc`, `www.wordpress.lxc`)"
        - "traefik.http.middlewares.wordpressredirectregex.redirectregex.regex=^http://wordpress.lxc/(.*)"
        - "traefik.http.middlewares.wordpressredirectregex.redirectregex.replacement=http://www.wordpress.lxc/$${1}"
        - "traefik.http.routers.www-wordpress.entrypoints=web"
        - "traefik.http.services.www-wordpress.loadbalancer.server.port=80"
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    environment:
      PMA_HOST: mariadb
      MYSQL_ROOT_PASSWORD: example
    depends_on:
      - mariadb
    networks:
      - net
      - proxynetwork
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.phpmyadmin-wordpress.rule=Host(`phpmyadmin.wordpress.lxc`)"
        - "traefik.http.routers.phpmyadmin-wordpress.entrypoints=web"
        - "traefik.http.services.phpmyadmin-wordpress.loadbalancer.server.port=80"
  phpfpm:
    image: koromerzhin/phpfpm:7.4.12
    depends_on:
      - mariadb
      - mercure
      - mailhog
    volumes:
      - ${PWD}/apps:/var/www/
      - ${PWD}/conf/httpd.conf:/usr/local/apache2/conf/httpd.conf
    networks:
      - net
    deploy:
      labels:
        - "traefik.enable=false"
